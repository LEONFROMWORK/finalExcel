[build]
builder = "NIXPACKS"
buildCommand = "./bin/railway-build.sh"

[deploy]
startCommand = "bundle exec rails server -b 0.0.0.0 -p ${PORT:-3000}"
healthcheckPath = "/up"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[services]]
name = "web"